import Section from './Section';

export default function StatementSection() {
  return (
    <Section id="statement" bg="bg-stone-500">
      <p className="max-w-3xl text-center text-3xl font-black">
        hack nights brings creative minds together to build and experiment with
        new ideas.
      </p>
    </Section>
  );
}